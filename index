<html><head><base href="." />
<style>
  body {
    background-color: #000;
    color: #0f0;
    font-family: 'Courier New', monospace;
    margin: 0;
    padding: 20px;
    line-height: 1.6;
    overflow-x: hidden;
  }

  .terminal {
    border: 2px solid #0f0;
    padding: 20px;
    margin: 20px auto;
    max-width: 800px;
    position: relative;
  }

  .cursor {
    display: inline-block;
    width: 10px;
    height: 20px;
    background: #0f0;
    animation: blink 1s infinite;
  }

  @keyframes blink {
    0%, 49% { opacity: 1 }
    50%, 100% { opacity: 0 }
  }

  .ascii-art {
    white-space: pre;
    font-size: 14px;
    line-height: 1.2;
    margin: 20px 0;
    animation: rainbow 5s infinite;
  }

  @keyframes rainbow {
    0% { color: #0f0; }
    25% { color: #0ff; }
    50% { color: #ff0; }
    75% { color: #f0f; }
    100% { color: #0f0; }
  }

  .button {
    background: #000;
    color: #0f0;
    border: 1px solid #0f0;
    padding: 10px 20px;
    cursor: pointer;
    transition: all 0.3s;
    margin: 10px;
  }

  .button:hover {
    background: #0f0;
    color: #000;
  }

  .matrix-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: -1;
  }

  #tokenomics {
    border: 1px solid #0f0;
    padding: 20px;
    margin: 20px 0;
  }
</style>
<title>FWOG AI - The Cutest AI Meme Token</title>
</head>
<body>

<div class="matrix-bg" id="matrix"></div>

<div class="terminal">
  <h1>> FWOG AI Token_<span class="cursor"></span></h1>
  
  <div class="ascii-art" id="frog">
    /\_/\
   (˃̵ᴗ˂̵)
    / >🪙
   </div>

  <p>Welcome to $FWOG AI - The First Amphibian-Powered AI Token</p>
  
  <div id="tokenomics">
    <h2>> Tokenomics_</h2>
    <p>Total Supply: 1,000,000,000 $FWOG</p>
    <p>Tax: 0/0</p>
    <p>Liquidity: Locked Forever</p>
    <p>Contract: 0xFw0g...420</p>
  </div>

  <div class="button-container">
    <button class="button" onclick="window.location.href='https://app.uniswap.org'">Buy on Uniswap</button>
    <button class="button" onclick="window.location.href='https://t.me/fwogai'">Join Telegram</button>
    <button class="button" onclick="window.location.href='https://twitter.com/fwogai'">Follow Twitter</button>
  </div>
</div>

<script>
// Matrix rain effect
const canvas = document.createElement('canvas');
const ctx = canvas.getContext('2d');
document.querySelector('.matrix-bg').appendChild(canvas);

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

const chars = 'FWOGAI$0123456789';
const drops = [];
const fontSize = 14;
const columns = canvas.width/fontSize;

for(let x = 0; x < columns; x++) {
    drops[x] = 1;
}

function draw() {
    ctx.fillStyle = 'rgba(0, 0, 0, 0.05)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    
    ctx.fillStyle = '#0f0';
    ctx.font = fontSize + 'px monospace';

    for(let i = 0; i < drops.length; i++) {
        const text = chars[Math.floor(Math.random() * chars.length)];
        ctx.fillText(text, i*fontSize, drops[i]*fontSize);
        
        if(drops[i]*fontSize > canvas.height && Math.random() > 0.975)
            drops[i] = 0;
        
        drops[i]++;
    }
}

setInterval(draw, 35);

// Make the frog hop
const frog = document.getElementById('frog');
let hopDirection = 1;

function hopFrog() {
    frog.style.transform = `translateY(${-10 * hopDirection}px)`;
    setTimeout(() => {
        frog.style.transform = 'translateY(0)';
    }, 500);
}

setInterval(hopFrog, 2000);

// Terminal typing effect
let currentText = '';
const texts = [
  'Loading FWOG AI...',
  'Initializing cute protocols...',
  'Ribbit ribbit...',
  'AI systems: OPERATIONAL'
];

let textIndex = 0;
let charIndex = 0;

function typeEffect() {
  if (charIndex < texts[textIndex].length) {
    currentText += texts[textIndex].charAt(charIndex);
    charIndex++;
    setTimeout(typeEffect, 100);
  } else {
    setTimeout(() => {
      currentText = '';
      charIndex = 0;
      textIndex = (textIndex + 1) % texts.length;
      typeEffect();
    }, 2000);
  }
}

typeEffect();
</script>
</body>
</html>
